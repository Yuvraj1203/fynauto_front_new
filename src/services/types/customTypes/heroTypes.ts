export enum CustomColor {
  default = "default",
  primary = "primary",
  secondary = "secondary",
  success = "success",
  warning = "warning",
  danger = "danger",
}

export enum CustomSize {
  sm = "sm",
  md = "md",
  lg = "lg",
}

export enum CustomRadius {
  none = "none",
  sm = "sm",
  md = "md",
  lg = "lg",
  full = "full",
}
